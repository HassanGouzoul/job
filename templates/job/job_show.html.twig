{% extends 'base.html.twig' %}
{% block stylesheets %}
    {#{{ parent() }}#}
    <h1>Offre N° {{ job.id }}</h1>
    <h2>{{ job.title }}</h2>
    <h3>{{ job.adresse }}</h3>
    <p>{{ job.description }}</p>

{% endblock %}
{% block javascripts %}
    {{ parent() }}
{% endblock %}
